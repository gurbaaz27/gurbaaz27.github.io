# Legacy Jekyll source

This directory preserves the pre-Astro Jekyll implementation for reference. It intentionally excludes generated `_site` output.

The original static assets were moved without modification to [`../public/assets`](../public/assets) so the Astro site and this reference snapshot do not duplicate 22 MB of media. The original top-level README is preserved as `README.original.md`.
